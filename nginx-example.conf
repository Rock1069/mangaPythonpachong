# Nginx 反向代理配置示例
# 文件路径: /etc/nginx/sites-available/manga-reader
# 启用: sudo ln -s /etc/nginx/sites-available/manga-reader /etc/nginx/sites-enabled/
# 重载: sudo nginx -t && sudo systemctl reload nginx

# ---- HTTP (仅IP/无域名) ----
server {
    listen 80;
    server_name _;

    # 客户端上传大小限制（如有）
    client_max_body_size 10m;

    # 代理到 Python 服务
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置（图片代理可能需要较长时间）
        proxy_read_timeout 60s;
        proxy_connect_timeout 30s;

        # 缓冲
        proxy_buffering off;
    }

    # 静态文件缓存
    location /static/ {
        proxy_pass http://127.0.0.1:8000/static/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 图片代理缓存优化
    location /api/proxy/image {
        proxy_pass http://127.0.0.1:8000;
        proxy_cache_valid 200 1d;
        add_header X-Cache-Status $upstream_cache_status;
    }
}

# ---- HTTPS (有域名时，使用 Let's Encrypt) ----
# 安装 certbot: sudo apt install certbot python3-certbot-nginx
# 获取证书:   sudo certbot --nginx -d your-domain.com
#
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#
#     location / { ... 同上 ... }
# }
#
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$host$request_uri;
# }
